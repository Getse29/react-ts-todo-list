import { Form } from './Form';

const Todo = () => {
  return (
    <div className="container">
      <div className="title">
        <h1>Todo List</h1>
        <Form />
      </div>
    </div>
  );
};

export default Todo;
